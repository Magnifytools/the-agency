# The Agency - Production Environment Variables
# Copy this file to your secret manager / production env and fill real values.

# Required
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/the_agency
SECRET_KEY=replace-with-a-strong-random-secret-at-least-32-chars
PRODUCTION=true

# JWT
ACCESS_TOKEN_EXPIRE_MINUTES=480
ALGORITHM=HS256

# Cookies / Session security
AUTH_COOKIE_NAME=agency_access_token
CSRF_COOKIE_NAME=agency_csrf_token
AUTH_COOKIE_SECURE=true
# Use "lax" when frontend/backend share same site.
# Use "none" only if frontend is on a different site/domain (requires HTTPS).
AUTH_COOKIE_SAMESITE=lax
AUTH_COOKIE_DOMAIN=
AUTH_COOKIE_PATH=/

# CORS (comma-separated)
# Example: https://agency.magnify.ing,https://app.magnify.ing
CORS_ORIGINS=https://agency.magnify.ing

# Optional but recommended for multi-worker rate limiting
REDIS_URL=redis://username:password@host:6379/0

# Optional integrations
DISCORD_WEBHOOK_URL=
ANTHROPIC_API_KEY=
HOLDED_API_KEY=

# Business defaults
DEFAULT_HOURLY_RATE=40.0

# Platform-provided in many deployments (Railway, etc.)
PORT=8004
